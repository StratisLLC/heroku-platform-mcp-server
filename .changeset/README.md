# Changesets

This directory contains changesets for the Heroku MCP monorepo. Each release-worthy change should add a markdown file here (created via `pnpm changeset`). Changesets are consumed at version time by `pnpm version`, then deleted.

See https://github.com/changesets/changesets for details.
