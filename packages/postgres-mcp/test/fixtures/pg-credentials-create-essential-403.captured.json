{
  "_source": "LIVE-CAPTURED 2026-06-09 against dm-pgtest (essential-0). POST /postgres/v0/databases/{name}/credentials (HTTP Basic). Status 403. Named credentials require standard-tier+ plans; the live integration test asserts THIS behavior for create on the essential test DB.",
  "message": "Cannot create new credentials for essential-tier addons."
}
