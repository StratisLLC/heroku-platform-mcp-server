{
  "_source": "MOCKED. DELETE /postgres/v0/databases/{name}/credentials/{name} returns 204 No Content (empty body) on success. dm-pgtest is essential-0 and only has the default credential (which cannot be destroyed), so a live destroy of a named credential could not be exercised on the test plan. The tool returns a synthesized {deleted:true,...} envelope for the empty 2xx.",
  "deleted": true,
  "credential": "mcp-test-cred",
  "database": "postgresql-dimensional-08540"
}
